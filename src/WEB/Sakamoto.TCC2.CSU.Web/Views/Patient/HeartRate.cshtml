@model Sakamoto.TCC2.CSU.Web.Models.Patients.UpdatePatientHeartRateViewModel

@{
    ViewData["Title"] = "HeartRate";
}

<h1>HeartRate</h1>

<h4>PatientViewModel</h4>
<hr/>
<div class="row">
    <div class="col-md-4">
        <form asp-action="HeartRate">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="HeartRate" class="control-label"></label>
                <input asp-for="HeartRate" class="form-control"/>
                <span asp-validation-for="HeartRate" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Id"/>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary"/>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}